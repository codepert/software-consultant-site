const FaqCard = ({ title, children, id, index, setIndex }) => {
  // const handleSetIndex = (id) => index !== id && setIndex(id);
  // const handleSetIndex = (id) => index !== id && setIndex(id);
  return <>{/* <dvi></dvi> */}</>;
};

export default FaqCard;
